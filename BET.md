# Bet: secure deny/allow pytest fixtures

Offer: a small, original pytest fixture/assertion pack for teams checking that guest requests receive HTTP 401 and encoded path traversal is rejected without breaking a legitimate file request.

Price: free public reference artifact; optional support/custom adapter work can be offered separately. No paid listing or spending was used.

30-day path: publish the new public repository, make the README discoverable, and use the assertions as a compact starting point for defensive CI checks.

Human click: none required for local use. GitHub publication is the only external action.

Security boundary: local in-memory demo only; no third-party probing, exploit payloads, secrets, or filesystem traversal.

Repository: https://github.com/RNGBubba/secure-deny-allow-pytest-fixtures

Published commit: dfae6039cbebdd3faf53cc9c4788540b416fc860.

Verification is recorded in `artifacts/pytest.txt`; the DoneMeans receipt is created after the repository commit and binds the test command to that artifact.
